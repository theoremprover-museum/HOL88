\DOC mk_cons

\TYPE {mk_cons : ((term # term) -> term)}

\SYNOPSIS
Constructs a {CONS} pair.

\DESCRIBE
{mk_cons("t","[t1;...;tn]")} returns {"[t;t1;...;tn]"}.

\FAILURE
Fails with {mk_cons} if second term is not a list or if the first term is not
of the same type as the elements of the list.

\SEEALSO
dest_cons, is_cons, mk_list, dest_list, is_list.

\ENDDOC

\DOC mk_abs

\TYPE {mk_abs : ((term # term) -> term)}

\SYNOPSIS
Constructs an abstraction.

\DESCRIBE
{mk_abs "var","t"} returns the abstraction {"\var. t"}.

\FAILURE
Fails with {mk_abs} if first term is not a variable.

\SEEALSO
dest_abs, is_abs, list_mk_abs, mk_var, mk_const, mk_comb.

\ENDDOC

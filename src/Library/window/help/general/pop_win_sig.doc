\DOC pop_win_sig

\TYPE {pop_win_sig : void signal}

\SYNOPSIS
Raised after the top window is popped from the current stack.

\LIBRARY window

\DESCRIBE
When the window library is first loaded this signal has associated
with it the handler {\\(). PRINT\_STACK ()} which ensures that
the top window is reprinted every time it is popped.

\FAILURE
Never fails.

\SEEALSO
beg_stack_sig, end_stack_sig, set_stack_sig, cng_win_sig, psh_win_sig.

\ENDDOC

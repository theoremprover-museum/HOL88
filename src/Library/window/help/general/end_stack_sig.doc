\DOC end_stack_sig

\TYPE {end_stack_sig : string signal}

\SYNOPSIS
Raised after a window stack is ended.

\LIBRARY window

\DESCRIBE
This signal is raised after a window stack is ended.
It is raised with the name of the window stack that was just ended.

When the xlabel component is loaded a handler is associated with this
signal that will set the label of the Xterminal in which HOL is 
running to `NO CURRENT STACK' is the current stack is the one
that was ended.

\FAILURE
Never fails.

\SEEALSO
beg_stack_sig, set_stack_sig, pop_win_sig, cng_win_sig, psh_win_sig.

\ENDDOC
